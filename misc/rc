export PS1="\w \$ "
alias ls="ls -Fa"
alias sfs="sshfs truenas_admin@192.168.0.6:/mnt/kohut/kallirrhoe/ /mnt/server -o uid=$(id -u),gid=$(id -g)"
read -r _b < ~/.cache/current-palette
printf '%s' "$_b"
unset _b
